package org.leodreamer.sftcore.integration.ponder.api;

import net.createmod.ponder.api.scene.SceneBuildingUtil;

public interface SFTStoryBoard {
    void program(SFTSceneBuilder scene, SceneBuildingUtil util);
}
